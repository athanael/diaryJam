<h2>All Posts</h2>

<div class="posts-container">
  <%= render "new" %>
  <% @posts.each do |post| %>
    <div class="post-wrapper">
        <%= render "post", post: post, show_details_button: true %>
    </div>
  <% end %>
</div>
