<div class="post">
  <div class="post-avatar">
    <%= image_tag user.avatar_url, alt: "#{user.name}'s avatar" %> <!-- Remplace `user.avatar_url` avec ton avatar path helper -->
  </div>
  <div class="post-content">
    <p class="post-text"><%= post.content %></p> <!-- Remplace `post.content` avec ton actual content de la post -->
  </div>
</div>
